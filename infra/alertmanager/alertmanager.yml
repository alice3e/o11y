global:
  resolve_timeout: 5m

# Простая маршрутизация - все алерты в один канал
route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 5m
  receiver: 'telegram-channel'

receivers:
  - name: 'telegram-channel'
    telegram_configs:
      - bot_token_file: '/etc/alertmanager/token'
        chat_id: -1002852417922
        api_url: 'https://api.telegram.org'
        parse_mode: 'HTML'
        # 2. Устанавливаем флаг, чтобы получать уведомления о решенных проблемах
        send_resolved: true
        message: |
          {{/* Заголовок для всей группы алертов */}}
          <b>{{ .GroupLabels.alertname }}</b>
          {{/* Проходим по каждому алерту в группе */}}
          {{ range .Alerts }}
          <hr> {{/* Горизонтальная линия для разделения алертов */}}
          <b>{{ if eq .Status "firing" }}🔥 Firing{{ else }}✅ Resolved{{ end }}</b>
          <b>Summary:</b> {{ .Annotations.summary }}
          <b>Description:</b> {{ .Annotations.description }}
          <b>Instance:</b> <code>{{ .Labels.instance }}</code>
          <a href="{{ .GeneratorURL }}">Show in Prometheus</a>
          {{ end }}
